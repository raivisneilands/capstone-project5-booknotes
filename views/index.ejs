<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Notes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles\styles.css">
</head>
<body>
    <h1 class="display-4 text-center">Book Notes</h1>
    <h2 class="display-6 text-end mx-5">
        <%= date %>
    </h2>
    <form action="/add-note" method="post" class="text-center">
        <button class="btn btn-primary mx-3" type="submit">New Book Note</button>
    </form>
    <% if (notes.length !== 0) { %>
        <% notes.slice().reverse().forEach(note => { %>
            <hr>
            <div class="note">
                <div class="note-image">
                    <img src="https://covers.openlibrary.org/b/olid/<%= note.olid %>-M.jpg" alt="Book Cover" class="float-end">
                </div>
                <div class="note-text">
                    <div class="book-title display-6 fw-medium">
                        <%= note.book_name %> 
                        <p class="display-6">
                            by <%= note.author %>
                        </p>
                    <hr>
                    </div>
                    <div class="book-note">
                        <%= note.book_note %>
                    </div>
                    <div class="date-added">
                        <p>
                            Last updated: <%= note.date_added %>
                        </p>
                    </div>
                    <div class="buttons">
                        <form action="/edit" method="post" class="mx-3">
                            <input type="hidden" name="noteId" value="<%= note.id %>">
                            <button type="submit" class="btn btn-secondary">Edit</button>
                        </form>
                        <form action="/delete" method="post" class="mx-3">
                            <input type="hidden" name="noteId" value="<%= note.id %>">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
            </div>
        <% }); %> 
    <% } else { %>
        <h2 class="display-6 m-5">No notes added</h2>
    <% } %>
</body>
</html>